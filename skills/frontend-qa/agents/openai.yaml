interface:
  display_name: "Frontend QA"
  short_description: "Playwright QA with source, PRD, recorded journeys, roles, test data, downloads, QA gates, professional summaries, and regression plans"
  default_prompt: "Use $frontend-qa to run evidence-backed frontend QA, correlate results with source code when available, validate requirement coverage, record/replay business journeys, role/permission matrices, test data lifecycle, authorized download/export artifacts, product/ADR context, artifact integrity, then return the professional summary, triaged root-cause fixes, QA gate status, and a regression rerun plan."
