interface:
  display_name: "Frontend QA"
  short_description: "Professional frontend QA with proof guard"
  default_prompt: "Use $frontend-qa to run evidence-backed frontend QA, correlate with source/PRD when available, enforce the four-part API/UI data-mismatch proof gate, reject generic/path-only journey assertions as business proof, apply assertionSuggestions/testCases/riskRegister/riskAcceptance/claimGuard/reportContentAudit/journeyAssertionAudit/qaIntake/defectProof to avoid overclaims, guessing, noisy report wording, and weak defect scheduling, and return a concise executive decision brief with proof-ready fixes, QA sign-off, assertion-suggestion pack, formal test cases, release-risk acceptance, and regression steps."
