interface:
  display_name: "Frontend QA"
  short_description: "Professional frontend QA with proof guard"
  default_prompt: "Use $frontend-qa to run evidence-backed frontend QA, correlate with source/PRD when available, enforce the four-part API/UI data-mismatch proof gate plus explicit requirement/source-runtime binding, reject generic/path-only journey assertions as business proof, lead with QA intake and qa-intake.config.json rerun pack when PRD/product scope is missing, apply assertionSuggestions/businessJourneys/testCases/riskRegister/riskAcceptance/defectTickets/traceability/automationSpecs/evidenceBundle/qaStrategy/artifactIntegrity/claimGuard/reportContentAudit/journeyAssertionAudit/qaIntake/defectProof to avoid overclaims, missing evidence paths, guessing, noisy report wording, and weak defect scheduling, use defectTickets for bug filing, traceability for PRD-to-test-to-defect sign-off, businessJourneys for requirement-to-scenario rerun planning, automationSpecs for review-only Playwright regression drafts, evidenceBundle for shareable proof handoff, qaStrategy for run/defer/out-of-scope/block decisions, and proof-aware suggestions by default, and return a compact executive decision brief with proof-ready fixes, QA sign-off, assertion-suggestion pack, business journey scenario pack, formal test cases, automation draft pack, evidence handoff status, test strategy status, release-risk acceptance, and regression steps."
