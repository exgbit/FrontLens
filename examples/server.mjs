// Backwards-compatible launcher. The independently testable demo application lives
// under examples/demo-app so --source-root never scans FrontLens itself by mistake.
import './demo-app/src/server.mjs';
